To write an image using OpenCV, you use the cv2.imwrite() function.This function takes two 
arguments: the file name you want to save the image as and the image data you want to save.
The cv2.imwrite() function is used to save an image in OpenCV. It takes the filename as the 
first argument and the image data as the second argument. The image format is determined by  
the file extension.Remember to provide the correct file extension to indicate the desired 
